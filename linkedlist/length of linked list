class Solution:
    def getLength(self, head):
        # Your code goes here
        count=0
        temp=head
        while temp:
            count+=1
            temp=temp.next
        return count
